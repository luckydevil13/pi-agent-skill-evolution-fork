# 01: Тест-харнесс + выделение кодек-модуля SKILL.md

**What to build:** Расширение продолжает работать ровно как раньше (регистрация в pi, guardrail, статистика, proposal-цикл), но текстовые операции над SKILL.md — разбор и сборка frontmatter, yaml-квотирование, замена description, «патч ровно один раз и только в body» — переезжают в отдельный модуль с узким интерфейсом, который не зависит от pi и typebox в runtime. Параллельно создаётся тестовая инфраструктура: npm-скрипты `test` (node --test) и `typecheck`, devDependencies с типами pi/typebox, конвенция «ядро импортирует pi и typebox только как типы (import type)».

**Blocked by:** None (can start immediately)

**Status:** done

- [X] Расширение регистрируется в pi и все команды/операции ведут себя как до рефакторинга (поведение не изменилось, это чистый перенос)
- [X] Кодек-модуль не имеет runtime-импортов pi/typebox — его тесты гоняются `node --test` без установленных peer-зависимостей
- [X] Unit-тесты кодек-модуля зелёные: round-trip frontmatter (включая отсутствие/битость frontmatter), yaml-квотирование спецсимволов и переводов строк, замена description, сборка SKILL.md по имени/описанию/телу
- [X] Тесты патч-инварианта: «find встречается ровно один раз» в body; попытка изменить frontmatter через патч отклоняется; описание меняется только через замену description
- [X] `npm test` и `npm run typecheck` зелёные с чистого клона после `npm install`
